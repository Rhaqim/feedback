services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: worldgame
      POSTGRES_USER: worldgame
      POSTGRES_PASSWORD: worldgame
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
